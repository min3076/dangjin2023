'use strict';

console.clear();

// When ready to paint, trigger the animation.
window.requestAnimationFrame(function () {
    document.querySelector('.history_origin').classList.add('history-show');
});